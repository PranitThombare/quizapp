# Online QuizApp - Backend (Spring)

Short description: Spring Boot backend for Theo. Online QuizApp.

## Requirements
- Java 17+
- Maven 3.6+ or Gradle 7+
- (Optional) PostgreSQL / MySQL if using a DB

## Run (Maven)
```bash
mvn clean spring-boot:run
